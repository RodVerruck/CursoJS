// É possível jogar uma função dentro de uma variável!

let v = function(x) {
    return x*2
}

// A variável 'v' recebeu a função, que retorna o dobro do valor da variável

console.log(v(5))

// Sairá no console o dobro do valor, que neste caso será o 10.